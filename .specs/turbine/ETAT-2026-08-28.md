# NusterKit — état au 28/08/2026

Note de reprise après compaction de conversation. Branche `beta` de NusterKit,
branche `beta` de `metalizzsas/Hardware` (PR draft #27).

---

## 1. CORRIGÉ, en attente de release — Timeout Prisma sur les bacs

**Symptôme** (logs machine, ~9 s après le démarrage) :

```
ERROR: Fastify: PrismaClientKnownRequestError:
Invalid `prisma.container.findUnique()` invocation:
Socket timeout (the database failed to respond to a query within the configured timeout).
    at async Container.fetch_slot_data (build/containers/containers.js:120)
    at async Container.socket_data (build/containers/containers.js:144)
    at async ContainersRouter.socket_data (build/routers/containers-routers.js:12)
    at async Machine.socket_data (build/machine.js:152)
```

**Mécanisme.** `Machine.socket_data()` (`machine.ts:134`) construit le statut
complet et attend `container_router.socket_data()`, qui appelle
`Container.fetch_slot_data()` — soit **une `prisma.container.findUnique()` par
bac**, à chaque diffusion. `metalfog-m-2` en compte plusieurs (ox, rd, act, edi,
eff…). Prisma sur SQLite travaille sur **une seule connexion** : les requêtes se
sérialisent.

Ce qui est lu (`loadedProductType`, `loadDate`) ne change qu'au chargement ou
vidage d'un bac. Rien ne justifie de le relire en base au rythme du WebSocket.

**Pourquoi au démarrage.** Le disque est encore occupé (migrations Prisma, plus
le `chown -R /data` ajouté récemment à l'entrypoint) pendant que le kiosk et
l'UI se connectent et réclament chacun un statut complet. `DEBOUNCE_MS = 50`
dans `websocket-dispatcher.ts` fait repartir un flush 50 ms après chaque signal
`ws.dirty`, ce qui empile les salves.

**Non fatal** : le statut échoue et sera réémis au signal suivant.

**Vérifié** : `on_connect` (`websocket-dispatcher.ts:169-177`) appelle bien
`machine.socket_data()` — donc un statut *complet*, et une requête par bac, à
chaque connexion de client.

**Source oubliée à la première analyse.** Le gestionnaire de capteur
`level-min` (`containers.ts:69`) appelait `is_product_loaded()` — une requête de
plus — à *chaque* scan io tant que le capteur lisait 0. Un bac vide suffisait
donc à interroger la base toutes les 500 ms, et un automate injoignable (§2)
mettait toutes les entrées à 0 d'un coup : les deux défauts se nourrissaient.

**Correctif appliqué.** Le délai n'a pas été touché. La ligne persistée est
tenue en mémoire (`#product_document`), écrite au chargement et au déchargement,
et hydratée une seule fois — les appels concurrents partagent la même requête
(`#hydration`). Seule la donnée persistée est mise en cache : le temps de vie
restant reste calculé à chaque appel, donc il décroît toujours.

Test : `packages/turbine/src/containers/containers.unit.test.ts` (prisma mocké,
compteur de requêtes) — 21 diffusions et 8 clients simultanés ne déclenchent
qu'une requête ; le cache suit chargement et déchargement ; le temps restant
décroît sans relire.

---

## 2. CORRIGÉ, en attente de release — verrou `unreachable`

Commit `4148e483` sur `beta`, changeset `tidy-moons-behave`, suite verte
(17 fichiers, 82 tests).

Un seul ping raté latchait `unreachable` sur les handlers WAGO et EX260Sx, et
rien ne l'effaçait : `connect()` refusait toute tentative ultérieure,
`schedule_reconnect()` cessait de planifier, et les lectures renvoyaient 0
(WAGO) ou levaient (EX260Sx) pour la durée de vie du processus. Toutes les
conditions io passaient au rouge sur une machine dont les capteurs allaient
bien, et **seul un redémarrage rétablissait**. D'où l'irreproductibilité
constatée : rouge un jour, vert le lendemain après reboot.

Test : `packages/turbine/src/io/io-handlers/wago.unit.test.ts` (`ping` et
`modbus-serial` mockés, aucun réseau).

`revolution-pi.ts` n'avait pas le défaut — il sert de référence.

**Reste à faire** : beta.5 (merge de la PR de version), remontée des épinglages
côté Hardware, nouvelle draft release, réépinglage machine.

---

## 3. DÉFAUTS IDENTIFIÉS, NON CORRIGÉS

- **Initialisation des conditions io** (`pbr/pbr-security-condition.ts`).
  `#state` naît à `"error"` ; la branche `parameter` fait
  `this.state = statusBlock.data`, la branche io se contente de s'abonner à
  `io.updated.<gate>`. Comme `POST /cycle/:name` construit le PBR et diffuse
  dans la foulée, le premier affichage est rouge par construction, pas par
  mesure. Présent aussi dans la 2.3.4 (`PBRSecurityCondition.ts:19,47-56`).
  C'est ce qui rend « pas encore évalué » indiscernable de « violé ».

- **Message d'erreur trompeur.** La machine affiche « conditions de sécurité non
  remplies » quand la cause réelle est la perte de contact avec l'automate.
  Renvoyer 0 est le bon repli pour des entrées de sécurité ; le silence sur la
  cause ne l'est pas.

- **Le dev tombe sur un POST de confort.** En dev, turbine envoie sa
  configuration à `https://sim-server.nuster.localhost` ; si le proxy portless
  n'écoute pas sur 443 (il faut `portless proxy start --port 1355 --https` avec
  sudo), le `fetch failed` non capturé tue tout le processus.

- **`svelte-check` est mort** : erreur interne TypeScript
  (`RangeError: Map maximum size exceeded`) puis « 1249 FILES 0 ERRORS » en
  10 ms — résultat vide, pas un succès. Préexistant, non couvert par la CI.

- **`deepInsert` dupliqué** entre `turbine/src/addons/deep-insert.ts` et
  `simulation-server/src/deepInsert.ts`. Rien ne force les deux copies à rester
  synchrones (le mode `remove` avait déjà divergé).

- **Job `Changeset` rouge sur chaque push `beta` sans rien à publier** :
  changesets interroge `npm info`, conclut à tort que la version locale n'est
  pas publiée, et prend un 403. La commande exacte rejouée hors runner renvoie
  pourtant la bonne liste. Cause non élucidée. Sans effet sur les images depuis
  qu'elles sont découplées (`if: !cancelled()`).

- **Aucun test ne vérifie qu'un conteneur démarre.** Un `docker run` de l'image
  turbine en CI, avec un `/data` neuf, répondant sur `/machine`, aurait attrapé
  d'un coup les trois blocages rencontrés : `openapi:extract`, corepack, et le
  `chown` de `/data`.

- **Poignée de main D-Bus — cause non élucidée.** `[DBusClient] D-Bus connection
  error (ignored): write EPIPE`, à chaque démarrage, ~200 ms après
  l'instanciation des contrôleurs. Le blocage de `/settings/network` qui en
  découlait est corrigé (voir §5), mais pas la raison de l'échec lui-même.

  **Éliminé** : les permissions de la socket. Sur la machine,
  `/host/run/dbus/system_bus_socket` est `srw-rw-rw-` root:root — accessible en
  écriture à n'importe quel uid. Le passage de turbine à l'utilisateur `nodejs`
  ne bute donc pas sur le fichier. Restent l'authentification EXTERNAL et la
  politique du bus.

  **Chronologie suspecte** : le `su-exec nodejs` date du 24/04/2026
  (`23c952e8`), le gestionnaire qui avale l'erreur D-Bus du 10/06/2026
  (`8ac31794`) — ajouté *après*, avec un commentaire qui attribue la panne à
  « une VM ou un hôte balena incomplet ». Piste à confirmer ou à écarter.

  **Expérience qui tranche** : dans le conteneur, comparer
  `node -e '<connexion systemBus>'` en root et via `su-exec nodejs`. Si seul le
  second échoue, c'est l'uid et le correctif est une politique de bus, pas du
  code. Depuis beta.7 le journal affiche la vraie raison : dbus-native renvoie
  la ligne de refus du démon brute, et non une `Error`, ce qui la faisait
  disparaître derrière l'EPIPE.

- **Verrou d'update balena — mécanisme non élucidé.** `Lock: Updates locking
  failed. Error: EACCES: permission denied, open '/tmp/balena/updates.lock'`,
  systématique à chaque démarrage sur la release `2.0.4-13ba0ad` (six boots
  entre 10:29 et 10:37 le 28/08), puis `Lock: Updates are now locked.` dès le
  premier boot de la release suivante — **sans** que l'entrypoint ait changé
  entre les deux images (beta.4 → beta.5). Le symptôme a donc disparu tout seul.

  Hypothèse non vérifiée : un `updates.lock` résiduel appartenant à root, hérité
  de l'époque où turbine tournait en root, dans un répertoire que le superviseur
  recrée à chaque nouvelle release. Deux tentatives d'inspection par
  `balena device ssh` ont échoué (pas de bash dans l'image Alpine pour la voie
  conteneur ; tunnel VPN sans réponse pour la voie hôte). À reprendre par
  `balena-engine inspect` depuis l'OS hôte, sur les `Mounts` du service.

  Le verrou n'est jamais relâché explicitement — aucun `unlock` dans le code, la
  libération se fait à la sortie du processus. C'est ce qui empêche le
  superviseur de remplacer le conteneur pendant un cycle : le perdre en silence
  est le vrai risque, l'`EACCES` n'était que journalisé.

  beta.6 (`3610672a`) donne `/tmp/balena` à `nodejs` dans l'entrypoint. C'est un
  garde-fou explicite, **pas** la correction de ce qui a été observé — à ne pas
  présenter comme tel.

---

## 4. ÉTAT DE LA CHAÎNE DE RELEASE

- npm : `@nuster/turbine@2.4.0-beta.6`, dist-tag `beta` ; `latest` reste 2.3.4
- Docker Hub : `turbine` et `ui` en `2.4.0-beta.5` et `2.4.0-beta.6`, plus le
  tag mobile `beta`. Les images `simulation-*` suivent leur propre version 1.2.x
  — elles ne portent pas les tags `2.4.0-beta.*`, et c'est normal.
- Hardware `beta` : épinglé `2.4.0-beta.5` (`19f9f0e`) — **pas encore beta.6**
- balena : draft `2.0.4-4c9d96c` (id 4289841, commit
  `579e2b40f4a36938e362c89bd30008b7`) sur `romain_sapet/nusterturbine`, tag
  `pull_request_id = 27`
- Machine de test : `86e6cac1321fb604097bd6f32630dc41`, configurée en
  `metalfog-m-2` + addon `manual-pressure-regulators`. Épinglée sur
  `579e2b40`, opérationnelle, tourne beta.5.

**Vérifié sur la machine (beta.5)** : plus de `Socket timeout` Prisma au
démarrage. L'EX260Sx se déconnecte toutes les 120 s (timeout de session ENIP) et
n'est rerouvert qu'à la prochaine écriture — normal pour un îlot de
distributeurs, qui n'a pas d'entrées. C'est très probablement ce cycle de
déconnexion qui armait le verrou `unreachable` du §2.

**Pas encore vérifié** : un cycle « Remplissage automatique activant » réel, avec
des conditions de sécurité qui suivent l'état des capteurs. C'est la validation
qui compte et elle demande la machine physique.

**Avant tout merge vers `main`** : la branche épingle des prereleases partout
(`@nuster/turbine`, les deux images). Il faut passer en 2.4.0 finale, après
`changeset pre exit` côté NusterKit.

---

## 5. CORRIGÉ LOCALEMENT, NON POUSSÉ

Quatre commits sur `beta` en local, avec leurs changesets. Ils formeront beta.7.

- `657495b2` — le POST de configuration vers `SIMULATION_URL` n'avait pas de
  `.catch()` : le rejet non géré tuait tout le process turbine, donc le dev ne
  démarrait pas sans serveur de simulation. C'était le défaut « le dev tombe sur
  un POST de confort » du §3.

- `0bc308a1` — écran E/S actionnable au tactile : lignes groupées par catégorie
  (`gates.categories.*`, données déjà présentes et inutilisées), lignes de 56 px,
  zone tactile de l'interrupteur portée à 44 × 56 px par pseudo-élément. Deux
  interrupteurs voisins passent de 28 à 56 px d'écart. Vérifié dans le navigateur
  à 1024 × 600, la résolution du kiosk.

- `f3353b97` — `/settings/network` ne chargeait jamais puis rendait 500.
  `dbusInvoker` n'avait aucun délai de garde, et dbus-native empile les messages
  dans un tableau tant que sa poignée de main n'a pas émis `connect` : si
  l'authentification échoue, rien n'est envoyé et aucun callback n'est rappelé.
  Appels bornés à 10 s, connexion morte jetée et rouverte à la requête suivante.
  Les scans du constructeur de `NetworkRouter` avalent leurs erreurs — sans ça,
  le délai transformait un blocage en process mort au boot.
  Test : `packages/turbine/src/dbus/dbus.unit.test.ts`, vérifié rouge avant
  (deux des trois cas échouaient par expiration du test lui-même).

- `e7e4e3fe` — cette note.

Publication npm par trusted publishing OIDC (plus de `NPM_TOKEN`) ; les images
se construisent dès qu'une version manque au registre, indépendamment de la
publication npm.
