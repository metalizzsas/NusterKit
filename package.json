{
  "name": "@metalizzsas/nuster-kit",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",

    "lint": "pnpm -r --parallel run lint",
    "test": "pnpm -r --parallel run test",
    
    "build": "pnpm -r --parallel --filter='./libs/*' run build && pnpm -r --parallel --filter='./packages/*' run build && pnpm -r --parallel --filter='./simulation/*' run build",

    "build-typings": "pnpm -r --parallel --filter='./libs/typings/' run build",

    "release": "changeset publish"
  },
  "keywords": [],
  "author": "@metalizzsas",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git@github.com:metalizzsas/NusterKit.git"
  },
  "devDependencies": {
    "@svitejs/changesets-changelog-github-compact": "^0.1.1",
    "@changesets/cli": "^2.25.0"
  },
  "packageManager": "pnpm@7.13.0"
}
