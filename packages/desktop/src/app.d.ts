declare namespace App
{
    interface Locals {
        /** Is machine screen, in dev mode, will always be true */
        is_machine_screen: boolean;
    }
    
    interface PageData {
        /** Is machine screen, in dev mode, will always be true */
        is_machine_screen: boolean;
    }
}